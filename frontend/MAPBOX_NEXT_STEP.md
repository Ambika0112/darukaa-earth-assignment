# Mapbox next step

Install dependencies with:

npm install

Add your token to `.env`:

VITE_MAPBOX_TOKEN=your_token

The challenge requires polygon drawing with Mapbox GL JS. Add the map component after the basic dashboard is working.
